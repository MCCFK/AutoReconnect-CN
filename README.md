# AutoReconnect-CN
此模组让你在服务器断线时自动重连，并可以在加入服务器后自动发送几条指令。配置界面简洁，支持多种语言。This mod automatically reconnects you when you get disconnected from the server, and can automatically execute several commands after joining the server. It features a clean configuration interface and supports multiple languages.
